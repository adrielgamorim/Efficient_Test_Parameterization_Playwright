{
  "name": "test-parameterization-demo",
  "version": "1.0.0",
  "type": "module",

  "devDependencies": {
    "@playwright/test": "^1.42.1",
    "typescript": "^5.0.0",
    "allure-playwright": "^2.15.1",
    "allure-commandline": "^2.25.0"
  },
  "scripts": {
    "test": "playwright test",
    "test:demo": "playwright test test/demo.spec.ts",
    "test:non-param": "playwright test test/non-parameterized.spec.ts",
    "test:when-not": "playwright test test/when-not-to-parameterize.spec.ts",
    "report": "allure generate allure-results --clean && allure open",
    "cleanup": "scripts\\cleanup.bat",
    "cleanup:full": "scripts\\cleanup.bat full"
  }
}